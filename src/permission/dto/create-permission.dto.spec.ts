import { CreatePermissionDto } from './create-permission.dto';

describe('CreatePermissionDto', () => {
  it('should be defined', () => {
    expect(new CreatePermissionDto()).toBeDefined();
  });
});
