import { UpdatePermissionDto } from './update-permission.dto';

describe('UpdatePermissionDto', () => {
  it('should be defined', () => {
    expect(new UpdatePermissionDto()).toBeDefined();
  });
});
